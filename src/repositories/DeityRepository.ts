/** Compatibility alias; selection and Sigil policy are implemented by the service. */
export { DeityService as DeityRepository } from '../services/DeityService.js';
export type { DeityRosterRow, OwnedDeityProgress } from '../services/DeityService.js';
