/** Compatibility alias; reward and progression policy belongs to the service. */
export { RaidRewardService as RaidRewardRepository } from '../services/RaidRewardService.js';
export type { RaidRewardGrant, RaidRewardResult } from '../services/RaidRewardService.js';
